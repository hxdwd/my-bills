# 2026-08-31 工作日志

## 处理支付宝账单导出（2026-07 ~ 2026-08）
- 源文件：`history-bills/bills_202607-202608.csv`（GBK，前 10 行为“特别提示”，真实表头第 11 行；8 列：记录时间,分类,收支类型,金额,备注,账户,来源,标签）。这是**支付宝导出**，与旧 APP 9 列格式不同。
- 复制并改造 `process_history_csv.py` → 新建 `process_alipay_202607_202608.py`，输出 `import_bills_alipay_202607-202608.csv`（列与 import_bills.csv 一致）。
- 分类映射按**当前线上库**核对（MCP 查 categories 表）：旧脚本里的 `房租水电` 现已改为 `居家`(子:房租/水电网/家居)，`旅游`子分类也变（玩乐/餐饮等），旧映射已失效，故重做映射。
- 跳过 7 条非真实消费：转账(4) + 金融保险/美团月付还款(2) + 1 条脏数据（其他分类、备注为 `7.40989E+27` 科学计数法）。注意：脏数字在**备注列**而非金额列，需单独判。
- 结果：写入 122 条，校验 0 非法分类 / 0 空日期 / 全 expense。分布：饮食102(正餐53/居家饮食27/零食饮料21/大餐1)、交通9、生活用品8、衣服鞋包1、娱乐1、通讯会员1。
- 提醒：本批次商户（沃尔玛/蜜雪/塔斯汀/哈啰/高德）不在 gen 脚本 tags 白名单内，生成 SQL 后 tags 基本为空，属正常。
- 生成的 import CSV 尚未跑 gen_import_sql 生成 SQL，也未入库；待用户确认后做下一步。

## 生成 SQL 并入库（同日续）
- 复制并改造 `gen_import_sql_history.py` → `gen_import_sql_alipay_202607_202608.py`，输出**带日期**的两个文件：
  - `import_tags_alipay_202607-202608.sql`（12 个白名单标签，ON CONFLICT DO NOTHING；本批次商户无白名单命中，故实际不写 tags，文件保留作记录）
  - `import_transactions_alipay_202607-202608.sql`（122 条 INSERT，按名字 JOIN 解析 category/subcategory，账户统一绑支付宝 `4493ef5b-...`）
- 修正：merchant 与 note 相同时不重复拼接（避免 "X - X"）。
- 入库：用 MCP execute_sql 分批执行 transactions SQL（分两批 DO 块，各 ~61 条），均返回 `[]` 无报错。
- 核验：2026-07~08 区间交易由导入前基线 3938 增至现 4060；区间现有 125 条 = 本次 122 + 区间原有 3 条（正常 app 记录，均在饮食），分布与预期一致（饮食105/交通9/生活用品8/衣服鞋包1/通讯会员1/娱乐1）。
- 所有生成文件均带日期后缀，符合"后期可查阅"要求。未提交 git（用户未要求）。

## 导入支出联动支付宝余额（同日续2）
- 用户要求：导入的支出要对应减少支付宝余额（默认账户是支付宝）。
- 核验：支付宝账户 `4493ef5b-...` 导入前余额 `77807.47`；今日导入批次（created_at 2026-08-31）恰好 122 条、合计 `1727.13`，与 CSV 总额一致（区间 125 条 1898.49 − 原有 3 条 171.36 = 1727.13）。
- 执行：原子 `UPDATE accounts SET balance = balance - (子查询批次总额)` → 现 `76080.34`（77807.47 − 1727.13）。`支付宝基金`(investment) 不受影响。
- 说明：2 条火车票（171+163，原 CSV 账户=招商银行）此前按用户确认"账户统一绑支付宝"已绑到支付宝，故一并计入支付宝扣减。若用户希望那 2 条改走招商银行余额，需另做账户归属修正。

## 为导入记录补打标签（同日续3）
- 用户要求：导入记录多数无标签，按备注信息 + 库里现有标签补打（只用现有标签，不新建）。
- 库内相关标签：塔斯汀/蜜雪冰城/沃尔玛/瑞幸/琴大妈/便利店/潮客到/零食舱/华莱士/拼多多等。
- 原子 DO 块为 31 条原本 tags=NULL 的导入记录按备注商家匹配打标。结果：本批次（created_at 2026-08-31）共 123 条（122 导入 + 1 条疑似今日 App 手记），其中 41 条有标签、82 条无。
- 各标签命中：沃尔玛15(12新+3旧)、蜜雪冰城9(7新+2旧)、塔斯汀5(3新+2旧)、琴大妈3、便利店3、零食舱2(1新+1旧)、潮客到1、瑞幸1、拼多多1(旧)、华莱士1(旧)。
- 仍无标签者多为小摊/个人收钱码/无对应库标签的商家（哈啰、高德、深圳通、好想来、零食有鸣、零食很忙、正新鸡排、必胜客、喜姐螺蛳粉、火车票、话费充值等）；因"只用现有标签"未新建。若用户要覆盖这些，需先新建对应标签。
