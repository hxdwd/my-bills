# 2026-07-17

- 修复 Reports.tsx 时间选择下拉面板"年份/月份分行"问题：根因是每块内部标签、数字、箭头竖向堆叠，视觉上变成「年/月」「份/份」两行。改为每块内部 `flex items-center justify-center gap-2` 整体横排（`年份 2026 ⌃⌄`），年份块与月份块外层 `flex items-center gap-3` 并排同一行。标签加 `whitespace-nowrap` 防折行。vite build 通过。
