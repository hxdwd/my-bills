# 2026-07-21

## 首页版本更新弹窗（新功能，v1.1.3）
- 需求：App 更新到新版本后，首页弹窗提示本次更新内容。
- 实现：`VERSION_LOGS` 抽到 `apps/pwa/src/data/versionLogs.ts` 作为全站唯一 CHANGELOG 源，`Settings.tsx` 与 `Home.tsx` 共用。`Home.tsx` 用 `useEffect` 比对 `__APP_VERSION__` 与 `localStorage['mybills:lastSeenVersion']`：记录存在且不同→弹 `Modal` 展示当前版本更新说明；首次安装（无记录）静默写入不弹；无论是否弹都写回当前版本，保证只提示一次。
- 版本号：`package.json` 1.1.2 → 1.1.3，`versionLogs.ts` 头部追加 1.1.3 条目。

## 同日其他改动
- 搜索页 `resolveRange` 补 `toYMD`，修复选时间区间整页崩溃。
- 资产页账户交互重构：点击行开只读账户信息面板（属性 + 相关 transfer 分页），编辑独立；去卡片右侧编辑按钮与标识颜色。
- 投资行"可用/持仓"中间副行改上下两行，右侧总值不变（首次误改右侧总值，用户澄清后返工）。

## 提交
- `62b67d2` 功能改动；`87ab405` summary.md 总结。均已 push main。

## 「人生进度」彩蛋页面（新功能，v1.1.4）
- 通用扩展表 `public.user_expand`（`user_id` 主键 + `extras jsonb`），迁移 `019_user_expand.sql` 已线上 apply。所有彩蛋数据放 `extras.life` 命名空间下，绝不改 `users`、绝不每彩蛋单建表。RLS 用 `public.get_current_user_id()`（读 `x-user-id` 头），**不能写 `auth.uid()`**（anon key 下恒 NULL）。已 `execute_sql` 复核策略正确。
- 前端数据读写 `services/userExpand.ts`：复用 sync-engine 的 `fetch + x-user-id` 直连模式，按 `user_id` upsert `extras`。
- 页面：`Egg.tsx`（设置→其他→彩蛋→`/egg` 卡片列表）、`LifeProgress.tsx`（`/egg/life`：生命大环 SVG / 4 条时间粒度进度条 / grid 周刻度横向滚动 / 多目标倒计时 rAF 滚动卡 + BottomSheet 录入）。动画：入场滑入、Canvas 星空 `StarField.tsx`、下拉随机名言（禁下拉同步）。文案禁用死亡/死期类负面词。
- 路由：`App.tsx` 注册 `/egg`、`/egg/life` 子页（自动隐藏 TabBar）。`Settings.tsx`「其他」加"彩蛋"入口（lucide `Sparkles`）。新增 `hooks/useHaptic.ts`（navigator.vibrate）。
- 避坑（本会话踩过）：① `PageContainer` 是 **named export**（`{ PageContainer }`，路径 `components/layout/PageContainer.tsx`），**没有 `header` prop**——header 须作为 children 渲染；② 之前项目里**没有 `useHaptic`**，已新建；③ 写 `data/lifeProgress.ts` 时文件一度被写空（0 字节），导致 `daysUntil` 报"未导出"，已整文件重写修复。
- 校验：`vite build` 通过（exit 0），新建文件 lint 0 错误。
- 提交 `55f6e7e` 已 push main；版本号 1.1.3 → 1.1.4，changelog 头部追加 1.1.4。
