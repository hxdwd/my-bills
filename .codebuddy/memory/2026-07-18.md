# 2026-07-18 每日记录

## 完成事项
- 报表页下拉刷新终版（SyncIndicator + SyncToast + usePullToRefresh 生命周期）已 commit push
- 交易编辑弹窗改进（分类限制类型 + 子分类内联标签）已 commit push
- 密钥泄露善后（filter-branch + force push + wrangler.toml 环境变量声明恢复）已完成
- 工作总结追加到 `docs/summary.md` 并 commit push
- 交易编辑 BottomSheet UI 优化（标签区域对齐 + 日期时间防截断）已完成
- 报表页年月选择器重构为 iOS 风格滚轮（WheelPicker 组件）已完成，未 commit

## 新增文件
- `apps/pwa/src/components/ui/WheelPicker.tsx` — iOS 风格滚轮选择器（惯性滑动 + 吸附对齐 + 3D 缩放淡出）

## 注意事项
- wrangler.toml 中的环境变量声明仅保留变量名+空字符串，实际密钥值在 Cloudflare Pages 控制台配置，不在仓库中
- 工作总结中不写入任何实际密钥值，仅描述事件和处理方式
